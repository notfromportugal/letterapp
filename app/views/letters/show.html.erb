<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/ ">
        <img alt="Home" id="logo" src="http://www.iconhot.com/icon/png/devine-icons-part-2/256/mail-2-w.png">
      </a>

      <div class="btn btn-default" id="viewlet">
        <% if current_user  %>
          <%= link_to "View Letters", user_letters_path(current_user) %>
        <% else %>
          <%= link_to "View Letters", new_session_path %>
        <% end %>
      </div>

      <div class="btn btn-default" id="compose">
        <% if current_user  %>
          <%= link_to "Compose", new_letter_path %>
        <% else %>
          <%= link_to "Compose", new_session_path %>
        <% end %>
      </div>

      <div class="btn btn-default" id="login">
        <% if current_user  %>
          <%= link_to "Logout", destroy_session_path %>
        <% else %>
          <%= link_to "Login", new_session_path %>
        <% end %>
      </div>
      
      <p class="navbar-text" id="navtext">
        <% if current_user  %>You are signed in as <%= current_user.email %></p> 
        <% else %>You are not signed in<% end %>
      </p>

    
    </div>
  </div>
</nav>



<h2> To: <em><%= @letter.reciever %></em> </h2>
<h2> From: <em><%= @letter.user.email %></em> </h2>


<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title"><%= @letter.title %></h3>
  </div>
  <div class="panel-body">
    <%= @letter.content %>
  </div>
</div>

<div class="btn btn-info">
	<%= link_to "Edit this Letter", edit_letter_path(@letter) %>
</div>

<div class="btn btn-info">
<%= link_to "Delete this Letter", letter_path, method: :delete, data: { confirm: "Are you sure you want to delete this letter permanently?"} %>
</div>

<div class="btn btn-success">
  <%= link_to "Send it off!", user_letters_path(current_user), data: {confirm: "App is currently in development. In the mean time, polish those communication skills."} %>
</div>


